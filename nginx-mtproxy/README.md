# nginx-mtproxy 批量部署脚本

一个功能完整的 nginx-mtproxy 批量部署脚本，支持自动镜像检查、端口冲突检测、批量部署等功能。

## 🚀 特性

- ✅ 自动检查并拉取最新 Docker 镜像
- 🔢 支持批量部署多个容器
- 🔧 自定义容器名称、端口、伪装域名
- 🚫 自动检测端口冲突
- 🎯 交互式配置界面
- 📊 详细的部署结果展示

## 📥 一键使用

```bash
# 下载并运行部署脚本
curl -fsSL https://raw.githubusercontent.com/pkdg503/stable-docker-installer/main/deploy-mtproxy.sh -o deploy-mtproxy.sh
chmod +x deploy-mtproxy.sh
./deploy-mtproxy.sh
```

## 🛠️ 手动下载

```bash
# 克隆仓库
git clone https://github.com/pkdg503/stable-docker-installer.git
cd stable-docker-installer

# 运行脚本
chmod +x deploy-mtproxy.sh
./deploy-mtproxy.sh
```

## 📋 使用流程

1. 运行脚本
2. 自动检查 Docker 环境和镜像
3. 输入要部署的容器数量
4. 配置基础参数（域名、起始端口）
5. 确认部署配置
6. 自动批量部署容器

## 🔧 管理命令

```bash
# 查看所有容器
docker ps -a --filter "name=nginx-mtproxy"

# 查看容器日志
docker logs <容器名称>

# 停止容器
docker stop <容器名称>

# 启动容器  
docker start <容器名称>

# 删除容器
docker rm -f <容器名称>
```

## ⚠️ 注意事项

- 需要 Docker 环境
- 需要 root 权限运行
- 确保端口未被占用
- 妥善保存生成的 Secret

## 📄 许可证

MIT License
